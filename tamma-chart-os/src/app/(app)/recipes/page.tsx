import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function RecipesPage() {
  return <PlaceholderPage title="สูตรอาหาร" phaseNote="วางแผนไว้ใน Phase 4 (สูตร + ต้นทุนเมนู)" />;
}
